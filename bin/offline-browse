#!/bin/bash

mkdir -p /tmp/archive
tar xvz -C /tmp/archive -f $1

PORT=$[ ( $RANDOM % 100 ) + 8000]
bash -c "sleep 3; open http://localhost:${PORT}" &
pushd /tmp/archive
python ArchiveHTTPServer.py $PORT
popd
